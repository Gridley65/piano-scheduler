rules_version = '2';

// Anyone signed in (you or your wife, both authenticated via Google Sign-In)
// can read and write. Nobody else can access anything.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
